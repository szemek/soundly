<div ng-app="project">
  <div ng-controller="TracksCtrl">
    <input type="text" ng-model="name" ng-change="search()" placeholder="Search">
    <table>
      <thead>
      <tr>
        <th>Track</th>
        <th>Artist</th>
        <th>Album</th>
        <th>Listened</th>
      </tr>
      </thead>
      <tbody>
      <tr ng-repeat="track in tracks" >
        <td>{{track.name}}</td>
        <td>{{track.artist.name}}</td>
        <td>{{track.album.name}}</td>
        <td>{{track.uts * 1000 | date:'yyyy-MM-dd HH:mm:ss Z'}}</td>
      </tr>
      </tbody>
    </table>
  </div>
</div>
