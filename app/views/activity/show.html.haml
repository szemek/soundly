%table
  - (0..23).each do |hour|
    %th= hour
  - ['Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday', 'Saturday', 'Sunday'].each do |day|
    %tr
      - (0..23).each do |hour|
        %td.span1= @data[day][hour]
